import { Routes } from '@angular/router';
import {LoginAuthComponent} from "./login-auth.component";
import {SIDEBAR_ROUTES} from "../side-bar/side-bar.routes";
export const LOGINAUYTH_ROUTES: Routes = [
  { path: '', component: LoginAuthComponent },
];
